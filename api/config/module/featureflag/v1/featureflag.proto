syntax = "proto3";

package clutch.config.module.featureflag.v1;

option go_package = "featureflagv1";

message Simple {
  map<string, bool> flags = 1;
}

message Config {
  oneof type {
    Simple simple = 1;
  }
}
