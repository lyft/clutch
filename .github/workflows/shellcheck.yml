name: scripts
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
    paths:
    - '**.sh'
    - .github/workflows/shellcheck.yml
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Shellcheck
      run: ./tools/shellcheck.sh
